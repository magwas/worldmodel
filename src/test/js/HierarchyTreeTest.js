/*global define: false, DOMParser: false, checkalert: false */
require([ "thing/HierarchyTree" ], function(
        HierarchyTree) {
    test("init", function() {
        checkalert();
        //window.tt.placeAt("navigatorArea");
        //(new HierarchyTree()).placeAt("navigatorArea");
    });

});
